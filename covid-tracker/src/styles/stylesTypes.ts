export type FlexContainerType = Partial<{
  margin: string;
  flexDirection: string;
  justifyContent: string;
  alignItems: string;
  gap: string;
}>;

export type CardType = Partial<{
  beforeColor: string;
}>;
